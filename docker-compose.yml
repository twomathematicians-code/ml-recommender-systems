services:
  api:
    build: .
    ports: ["9000:9000"]
    environment:
      REC_PORT: 9000
      MODEL_DIR: /models
    volumes:
      - model_data:/models
    restart: unless-stopped

volumes:
  model_data:
